---
import Particles from './Particles.astro'
import TextRotate from './TextRotate.astro'

import SocialLinks from '@components/SocialLinks.astro'

import { CldImage } from 'astro-cloudinary'
import { getEntry } from 'astro:content'

const heroimg = await getEntry('assets', 'assets/50_vi8xgl_gaxgjr')
---

<!--Header-->
<section class="relative block h-screen w-screen overflow-hidden ">
	<Particles />
	<!--Social Media Links-->
	<SocialLinks />
	<!--Social Media Links end-->
	<div
		class="absolute z-[1] float-left flex h-screen w-full flex-col items-center justify-center text-[50px] font-normal tracking-[1px]"
	>
		<div class="z-[2] h-auto w-auto text-center sm:pr-[70px] lg:pr-[700px]">
			<h1 class="z-[2] text-[65px] font-bold">
				<span class="font-extrabold text-[#b11141] transition-all duration-300 ease-in-out"
					>Steve
				</span>Moya
			</h1>
			<div class="z-[2] pt-5 text-[32px] font-bold text-whitesmoke">
				Soy
				<TextRotate
					period="1200"
					color="#b11141"
					text={['Desarrolador Web', ' y', 'Marketer Digital Specialis.']}
				/>
			</div>
		</div>
	</div>
	<!--header image-->
	<div class="float-right h-auto w-full bg-cover bg-center object-cover object-right">
		<CldImage
			class="float-right h-screen w-full object-cover [mask-image:linear-gradient(black_40%,transparent_100%)]"
			src={heroimg?.data.public_id || ''}
			loading="eager"
			alt="Steve-moya"
		/>
	</div>
	<!--header image end-->
</section>
<!--Header End-->

---

import { Picture, Image } from 'astro:assets'

interface Props {
	src: string
	alt: string
	widths?: number[]
	sizes?: string
	formats?: string[]
	fallbackFormat?: string
	class?: string
	loading?: 'lazy' | 'eager'
	quality?: 'low' | number
}

const {
	src,
	alt,
	formats = ['avif', 'webp'],
	loading = 'lazy',
	class: className,
	quality
} = Astro.props
---

<img
	src={src}
	alt={alt}
	loading={loading}
	class={className}
	quality={quality}
/>
